用户请求到来
    ↓
┌─────────────────────┐
│  1. 意图识别         │ ← 调用大模型提取意图（必须）（分析用户自然语言）
└─────────┬───────────┘
          ↓
┌─────────────────────┐
│  2. 检查缓存是否有效  │ ← is_expired() 判断
└─────────┬───────────┘
          ↓
     ┌────┴────┐
     │ 是否过期？│
     └────┬────┘
      Yes │   No
     ┌────┴────┐
     │         ↓
     │   ┌──────────────────┐
     │   │ 3. 使用缓存数据   │ ← 直接返回内存中的数据
     │   └────────┬─────────┘
     │            ↓
     │   ┌──────────────────┐
     │   │ 4. 按需加载上下文 │ ← 根据intent选择最少数据
     │   └────────┬─────────┘
     │            ↓
     │   ┌──────────────────┐
     │   │ 5. 生成回复       │ ← 调用大模型（token少）
     │   └──────────────────┘
     ↓
  ┌──────────────────┐
  │ 3. 查询数据库     │ ← 重新从DB读取
  └────────┬─────────┘
           ↓
  ┌──────────────────┐
  │ 4. 更新缓存       │ ← 保存到内存
  └────────┬─────────┘
           ↓
  ┌──────────────────┐
  │ 5. 构建上下文     │
  └────────┬─────────┘
           ↓
  ┌──────────────────┐
  │ 6. 生成回复       │ ← 调用大模型（token多）
  └──────────────────┘


2):
CACHE_USAGE.md - 完整的使用指南、配置说明、常见问题
IMPLEMENTATION_SUMMARY.md - 技术实现细节、性能数据、优化建议